apiVersion: v1
kind: Service
metadata:
  name: mdstechday-backend-nodejs
  namespace: backend
spec:
  selector:
    app: mdstechday-backend-nodejs
  ports:
   -  protocol: TCP
      port: 80
      targetPort: 3000

